# راهنمای نصب و اجرای ربات تلگرام 🚀

## قدم به قدم راه‌اندازی سیستم

### 1️⃣ نصب Tesseract OCR

#### دانلود و نصب:
1. به [صفحه دانلود Tesseract](https://github.com/UB-Mannheim/tesseract/wiki) بروید
2. فایل `tesseract-ocr-w64-setup-v5.x.x.exe` را دانلود کنید
3. فایل را اجرا کنید و مراحل نصب را دنبال کنید

#### نکات مهم نصب:
- ✅ مسیر نصب پیشنهادی: `D:\Program Files\Tesseract-OCR\`
- ✅ حتماً **Persian (فارسی)** را در لیست زبان‌ها انتخاب کنید
- ✅ گزینه "Add to PATH" را فعال کنید

### 2️⃣ تنظیم محیط Python

#### نصب پکیج‌ها:
```bash
# نصب پکیج‌های مورد نیاز
pip install pyautogui pytesseract Pillow schedule

# یا استفاده از فایل requirements
pip install -r requirements.txt
```

### 3️⃣ تست سیستم

#### اجرای تست:
```bash
python test_system.py
```

اگر تمام تست‌ها سبز شدند، سیستم آماده است! ✅

### 4️⃣ تنظیم مختصات (دو روش)

#### روش A: رابط گرافیکی (پیشنهادی) 🖱️
```bash
python gui_configurator.py
```

1. تب "تنظیم مختصات" را باز کنید
2. تلگرام دسکتاپ را باز کنید و به چت بروید
3. روی دکمه "تعیین گوشه بالا-چپ چت" کلیک کنید
4. ماوس را روی گوشه بالا-چپ ناحیه چت ببرید
5. همین کار را برای گوشه پایین-راست و جعبه ورودی انجام دهید
6. روی "ذخیره تنظیمات" کلیک کنید

#### روش B: تنظیم دستی ⌨️
فایل `config.py` را باز کنید و مقادیر زیر را تنظیم کنید:

```python
CHAT_REGION = {
    'x': 100,      # موقعیت افقی شروع
    'y': 200,      # موقعیت عمودی شروع
    'width': 800,  # عرض منطقه چت
    'height': 400  # ارتفاع منطقه چت
}

INPUT_CLICK_POSITION = {
    'x': 500,  # موقعیت افقی جعبه ورودی
    'y': 650   # موقعیت عمودی جعبه ورودی
}
```

### 5️⃣ اجرای ربات

#### روش‌های مختلف اجرا:

**A. اجرای مستقیم:**
```bash
python telegram_auto_reply.py
```

**B. استفاده از فایل batch:**
```
start_bot.bat
```

**C. از طریق رابط گرافیکی:**
- `gui_configurator.py` را اجرا کنید
- روی دکمه "🚀 اجرای ربات" کلیک کنید

## نکات مهم برای استفاده ⚠️

### آماده‌سازی تلگرام:
1. تلگرام دسکتاپ را باز کنید (نه وب یا موبایل)
2. به چت مورد نظر بروید
3. پنجره تلگرام در پیش‌زمینه باشد
4. اندازه فونت متوسط باشد (برای تشخیص بهتر متن)

### حین اجرا:
- ✅ تلگرام همیشه در پیش‌زمینه باشد
- ✅ پنجره‌های دیگر جلوی ناحیه چت نباشند
- ✅ ربات هر 10 ثانیه چک می‌کند (قابل تنظیم)
- ⚠️ برای توقف: `Ctrl+C`
- 🚨 برای توقف اضطراری: ماوس به گوشه صفحه

### بهینه‌سازی عملکرد:
1. **کیفیت تصویر**: رزولوشن صفحه بالا باشد
2. **نور محیط**: نمایشگر روشن باشد
3. **فونت**: اندازه متوسط تا بزرگ
4. **کنتراست**: تم تلگرام روشن بهتر از تاریک

## عیب‌یابی رایج 🔧

### مشکل: "Tesseract یافت نشد"
```
❌ راه‌حل:
- مسیر در config.py را بررسی کنید
- Tesseract مجدداً نصب کنید
- فایل tesseract.exe موجود باشد
```

### مشکل: "متن تشخیص داده نمی‌شود"
```
❌ راه‌حل:
- منطقه چت را مجدداً تنظیم کنید
- اندازه فونت تلگرام را بزرگ‌تر کنید
- تم روشن تلگرام استفاده کنید
- رزولوشن صفحه را بررسی کنید
```

### مشکل: "ربات پاسخ نمی‌دهد"
```
❌ راه‌حل:
- موقعیت جعبه ورودی را بررسی کنید
- تلگرام در پیش‌زمینه باشد
- فایل لاگ (telegram_bot.log) را بررسی کنید
- مجوزهای امنیتی ویندوز را بررسی کنید
```

### مشکل: "خطای import"
```
❌ راه‌حل:
pip install pyautogui pytesseract Pillow schedule
```

## فایل‌های پروژه 📁

```
telegram-auto-reply/
├── 📄 telegram_auto_reply.py    # اسکریپت اصلی ربات
├── ⚙️ config.py                 # فایل تنظیمات
├── 🛠️ setup.py                  # راه‌انداز خودکار
├── 🧪 test_system.py            # تست سیستم
├── 🖼️ gui_configurator.py       # رابط گرافیکی
├── 🚀 start_bot.bat             # اجرای آسان
├── 📝 requirements.txt          # پکیج‌های مورد نیاز
├── 📖 README.md                 # راهنمای کامل
└── 📋 INSTALL_GUIDE.md          # راهنمای نصب (این فایل)
```

## پشتیبانی و کمک 💬

### فایل‌های لاگ:
- `telegram_bot.log`: فعالیت‌های ربات
- خطاها و هشدارها در کنسول نمایش داده می‌شوند

### اطلاعات سیستم مورد نیاز:
- ویندوز 10/11
- Python 3.8+
- حداقل 4GB رم
- Tesseract OCR
- تلگرام دسکتاپ

### نسخه‌سازی:
- نسخه فعلی: 1.0.0
- آخرین بروزرسانی: آگوست 2025
- سازگار با: Windows 10/11

---

### ✨ ویژگی‌های نسخه آینده:
- 🔄 پشتیبانی از چندین چت همزمان
- 🧠 قوانین پاسخ‌دهی پیچیده‌تر
- 📊 آمارگیری از پیام‌ها
- 🎨 بهبود رابط گرافیکی
- 🌐 پشتیبانی وب‌اپ تلگرام

**موفق باشید! 🎉**
